/*export default class AvatarInfo {
    constructor(avatarInput) {
        this._avatar = document.querySelector(avatarInput.info);
        this._image = document.querySelector('.profile__avatar');
    }

    setData() {
        this._image.src = this._data.link;
        this._image.alt = this._data.name;
    }

    eventListeners() {
        this._image.addEventListener('click', () => this._onAvatarClick());
    }

    _onAvatarClick() {

    }

}

export default class AvatarInfo {
    constructor(avatarInput) {
        this._avatarImage = document.querySelector(avatarInput.link);
        this._avatarAlt = document.querySelector(avatarInput.alt);
    }

    getUserInfo() {
        return {
            link: this._avatarImage.src,
            alt: this._avatarAlt.alt
        }
    }

    setUserInfo(value) {
        this._avatarImage.
        this._avatarAlt
       
    }
}*/